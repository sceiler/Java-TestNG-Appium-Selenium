name: Selenium Web Simple Tests Non GCP

on:
  workflow_dispatch:
  schedule:
    - cron: '15 * * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Build with Maven
        run: mvn clean install compile test -Dsurefire.suiteXmlFiles=./src/test/resources/Selenium_Web_Simple_GCP_Long.xml -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn -B -V
        env:
          SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
          SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
          BUILD_TAG: Selenium-Simple-Non-GCP-Long-1.${{ github.run_number }}
          TAGS: GCP_TESTING,NON_GCP,SimpleTests,Long
      - name: Publish Test Report
        if: ${{ always() }}
        uses: scacap/action-surefire-report@v1
